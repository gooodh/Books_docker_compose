upstream books_django {
    server web:8000
}

server {
    listen 80;
    location / {
        include proxy_params;
        proxy_pass http://books_django;

    }
    location /static/ {
        alias /code/static/;
    }
    location /media/ {
        alias /code/media/;
    }
}
